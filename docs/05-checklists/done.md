# Checklist — DONE

Itens já concluídos no projeto:

- Remover import de `lovable-tagger` em `vite.config.ts`
- Remover meta tags e imagens do Lovable em `index.html`
- Remover referências a Lovable no `README.md`
- Remover `lovable-tagger` de `package.json` dependencies
- Remover import e uso do `logo` em `src/components/Navbar.tsx` e `Footer.tsx` (substituído por placeholders)
- Remover estilos `.logo` em `src/App.css`
- Criar pasta `/docs` e estrutura de subpastas
- Adicionar arquivos iniciais em `/docs` (contexto, roadmap, tasks, prompts, template)
- Criar `docs/design-system` com arquivos iniciais
- Atualizar documentos do design system com valores do projeto (tokens, tipografia, cores, components, spacing, usage)
- Documentar Motion & Effects em `docs/design-system/effects.md`
- Adicionar subtasks 002–010 no backlog em `docs/02-tasks/backlog/`
- Atualizar `docs/00-contexto/projeto.md` com o estado atual
- Criar documento `decisoes-produto-e-design.md`
- Criar `docs/07-integrations/README.md`
 - Remover seção de Depoimentos do fluxo da página (`src/pages/Index.tsx`)

Observações:
- Marque novas entregas aqui conforme forem concluídas; use os arquivos em `/docs/02-tasks/` para evidenciar progresso.
