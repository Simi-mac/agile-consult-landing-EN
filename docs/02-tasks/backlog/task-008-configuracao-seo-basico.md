```markdown
# Task 004 - Configuração de SEO básico

Status:
Backlog

Tipo:
Configuração / SEO

Pedido:
Configurar SEO inicial da página.

Contexto:
Site novo precisa de metadados básicos e estrutura de headings para indexação e preview em buscadores.

Objetivo:
Garantir que a página tenha title, meta description, headings e alt de imagens otimizados.

Escopo técnico:
- Definir `<title>` otimizado em `index.html`.
- Definir `<meta name="description">` em `index.html`.
- Estruturar `h1`, `h2`, `h3` nos componentes principais (Hero, seções).
- Adicionar/otimizar `alt` nas imagens em `src/components` e `src/assets`.
- Adicionar palavras-chave principais no meta tags (com moderação).

Critérios de aceite:
- Título da página otimizado
- Meta description definida
- Headings estruturados corretamente (um `h1`, hierarquia de `h2`/`h3`)

Arquivos possivelmente impactados:
- `index.html`
- `src/components/HeroSection.tsx`
- `src/components/*` (headings e alt)
- `src/assets/*` (imagens usadas)

Observações:
- Evitar keyword stuffing; priorizar UX e clareza para leitores humanos e buscadores.

``` 
